# **Houses multiple CSS animation exercises**

- **Transition Property:** Uses the transition property to animate a pink square. Triggers the animation using hover and by changing classes via JavaScript.
